import 'package:flutter/material.dart';

/// Form to create a new resident.
class ResidentCreatePage extends StatelessWidget {
  const ResidentCreatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Create Resident')),
      body: const Center(
        child: Text('Create resident form — coming soon'),
      ),
    );
  }
}
